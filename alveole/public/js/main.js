window.onload = function(){

  // Model part
  model = new Model();      // Create
  model.setValue(0);        // init



  controler = new Controler(model);
  // //set the value 0 to initial the page
}
